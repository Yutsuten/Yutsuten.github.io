---
---

### Create
```shell
touch file_name
```

### Remove
```shell
rm file
rm -r folder/
rm -rf folder/  # -f Do not ask for confirmation
```

### Create directory
```shell
mkdir new_directory
mkdir -p path/to/directory  # -p Parents (recursively)
```
