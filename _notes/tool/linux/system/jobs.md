---
---

## Job management

Type `Ctrl + Z` to stop the job.

```shell
jobs     # List all jobs
jobs -r  # Running jobs
jobs -p  # PID only

fg       # Resume the job
fg NUM

bg       # Resume the job in the background
bg NUM
```
