```shell
# Remove untracked files from the working tree
git clean

# Show what would be removed
git clean -n

# Remove untracked directories
git clean -d

# Force remove
git clean -f

# Clean only inside a folder
git clean [options] directory/
```
