```shell
yarn init                  # Create package.json
yarn                       # Install all
yarn add <package> [--dev] # Add a package
yarn remove <package>      # Remove a package
yarn upgrade               # Upgrade packages
yarn global add <package>  # Save a package globally
yarn test                  # Run tests
```
